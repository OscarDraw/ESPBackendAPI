server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/OscarDraw/configData.git
          default-label: main
          force-pull: true
          search-paths: configData
